interface:
  display_name: "Queue"
  short_description: "Manual prompt check with follow-ups"
  default_prompt: "Use $queue to check whether my request is clear enough before work begins."
policy:
  allow_implicit_invocation: false
