interface:
  display_name: "Orchestrate"
  short_description: "Delegate implementation and verify the result"
  default_prompt: "Use $orchestrate to delegate this implementation to sub-agents and independently verify the result."
policy:
  allow_implicit_invocation: false
